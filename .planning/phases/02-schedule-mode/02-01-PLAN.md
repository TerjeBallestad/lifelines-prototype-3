---
phase: 02-schedule-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/types.ts
  - src/stores/GameStore.ts
  - src/components/schedule/ScheduleGrid.tsx
  - src/components/schedule/TimeSlotRow.tsx
  - src/components/schedule/ScheduleCell.tsx
  - src/components/PatientCard.tsx
  - src/components/GameShell.tsx
  - package.json
autonomous: true

must_haves:
  truths:
    - "Player sees a 3x3 grid with time slots (Morning, Afternoon, Evening) as rows and patients as columns"
    - "Schedule grid is visible in Schedule mode below patient cards"
    - "Empty slots are visually distinct and ready to accept assignments"
  artifacts:
    - path: "src/models/types.ts"
      provides: "ScheduleEntry type definition"
      contains: "ScheduleEntry"
    - path: "src/stores/GameStore.ts"
      provides: "Schedule map and CRUD methods"
      contains: "schedule"
    - path: "src/components/schedule/ScheduleGrid.tsx"
      provides: "Main schedule grid container"
      min_lines: 30
    - path: "src/components/schedule/TimeSlotRow.tsx"
      provides: "Row for each time slot with patient cells"
      min_lines: 20
    - path: "src/components/schedule/ScheduleCell.tsx"
      provides: "Individual droppable cell for activity assignment"
      min_lines: 20
  key_links:
    - from: "src/components/schedule/ScheduleGrid.tsx"
      to: "src/stores/GameStore.ts"
      via: "useGameStore hook"
      pattern: "useGameStore"
    - from: "src/components/schedule/ScheduleCell.tsx"
      to: "src/stores/GameStore.ts"
      via: "getAssignment method"
      pattern: "getAssignment"
---

<objective>
Create schedule state infrastructure and the visual grid layout for activity assignment.

Purpose: Players need to see where they can assign activities before drag-drop is wired up. The schedule grid with time slots as rows and patients as columns establishes the core scheduling interface.

Output: Schedule data structure in MobX store, ScheduleGrid/TimeSlotRow/ScheduleCell components rendering a 3x3 grid.
</objective>

<execution_context>
@/Users/godstemning/.claude/get-shit-done/workflows/execute-plan.md
@/Users/godstemning/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-schedule-mode/02-CONTEXT.md
@.planning/phases/02-schedule-mode/02-RESEARCH.md

# Existing files to modify
@src/models/types.ts
@src/stores/GameStore.ts
@src/components/GameShell.tsx
@src/components/PatientCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dnd-kit and add schedule state to GameStore</name>
  <files>
    package.json
    src/models/types.ts
    src/stores/GameStore.ts
  </files>
  <action>
1. Install dependencies:
   ```bash
   npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities @headlessui/react
   ```

2. Add ScheduleEntry type to src/models/types.ts:
   ```typescript
   export interface ScheduleEntry {
     patientId: string;
     timeSlot: TimeSlot;
     activityId: string | null;
   }
   ```

3. Update GameStore with schedule state and methods:
   - Add `schedule: Map<string, string | null>` using observable.map()
   - Key format: `${patientId}-${timeSlot}` (e.g., "patient-1-morning")
   - Add methods:
     - `assignActivity(patientId: string, slot: TimeSlot, activityId: string | null): void`
     - `clearAssignment(patientId: string, slot: TimeSlot): void`
     - `getAssignment(patientId: string, slot: TimeSlot): Activity | null`
     - `clearAllAssignments(): void`

   Use `this.schedule.set(key, activityId)` for assignments. The getAssignment method should look up activityId from map, then return the Activity from activities array.

Important: Use observable.map() from mobx for the schedule, not a plain Map, to ensure reactivity.
  </action>
  <verify>
    - `npm list @dnd-kit/core` shows installed
    - TypeScript compiles without errors: `npx tsc --noEmit`
    - Schedule methods exist in GameStore
  </verify>
  <done>
    - Dependencies installed
    - ScheduleEntry type exists
    - GameStore has schedule map with assign/clear/get methods
  </done>
</task>

<task type="auto">
  <name>Task 2: Create schedule grid components</name>
  <files>
    src/components/schedule/ScheduleGrid.tsx
    src/components/schedule/TimeSlotRow.tsx
    src/components/schedule/ScheduleCell.tsx
    src/components/GameShell.tsx
  </files>
  <action>
1. Create src/components/schedule/ directory.

2. Create ScheduleCell.tsx:
   - Props: `{ patientId: string, timeSlot: TimeSlot }`
   - Observer component using `observer()` from mobx-react-lite
   - Renders a bordered div (min-h-16, rounded corners)
   - Uses gameStore.getAssignment() to check if activity assigned
   - If activity assigned: show activity name + energy cost (e.g., "Reading (-1)")
   - If empty: show placeholder text "Empty" in muted color
   - Add data attributes for later DnD: `data-patient={patientId}` `data-slot={timeSlot}`

3. Create TimeSlotRow.tsx:
   - Props: `{ timeSlot: TimeSlot, patients: Patient[] }`
   - Observer component
   - Renders a row with:
     - First column: time slot label (capitalize "morning" -> "Morning")
     - Subsequent columns: ScheduleCell for each patient
   - Use flex or grid layout with consistent column widths

4. Create ScheduleGrid.tsx:
   - Observer component
   - Gets patients and activities from gameStore
   - Renders:
     - Header row with patient names (align with columns)
     - TimeSlotRow for each slot: morning, afternoon, evening
   - Grid layout: first column for labels, then one column per patient
   - Use Tailwind: `grid grid-cols-[100px_repeat(3,1fr)] gap-2` for 3 patients

5. Update GameShell.tsx:
   - Import ScheduleGrid
   - In schedule mode, render ScheduleGrid below the PatientGrid
   - Layout adjustment: main content should show PatientGrid (compact) + ScheduleGrid (main focus)

Visual structure:
```
| Header (Day 1, Morning, Schedule Mode) |
|----------------------------------------|
|  Main Content          |   Sidebar     |
|  - PatientGrid (small) |   Activities  |
|  - ScheduleGrid        |               |
|    (time slots x patients)             |
```

Make sure observer() wraps all components that read from store.
  </action>
  <verify>
    - `npm run dev` and app loads without errors
    - Schedule grid visible with 3 rows (Morning, Afternoon, Evening) and 3 patient columns
    - Patient names appear in header row above their columns
    - All cells show "Empty" placeholder
    - TypeScript compiles: `npx tsc --noEmit`
  </verify>
  <done>
    - ScheduleGrid.tsx renders 3x3 grid with time slot labels and patient headers
    - TimeSlotRow.tsx renders one row with cells for each patient
    - ScheduleCell.tsx shows activity name when assigned or "Empty" when not
    - Grid is visible in GameShell when in schedule mode
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Run `npm run dev` and verify app loads
2. Schedule grid is visible with proper layout (3 time slots x 3 patients)
3. All cells show "Empty" state
4. Patient names visible in header
5. Time slot labels visible in first column
6. `npx tsc --noEmit` passes
</verification>

<success_criteria>
- SCHED-01 partial: Grid visible with 3 time slots x patients (assignment interaction in next plan)
- Schedule data structure ready for drag-drop wiring
- Components properly observe store state
</success_criteria>

<output>
After completion, create `.planning/phases/02-schedule-mode/02-01-SUMMARY.md`
</output>
