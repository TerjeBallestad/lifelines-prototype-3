---
phase: 04-day-end-summary
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/components/summary/DaySplash.tsx
  - src/components/summary/ContinueButton.tsx
  - src/components/summary/SummaryView.tsx
  - src/stores/SimulationStore.ts
autonomous: true

must_haves:
  truths:
    - "Player can click Continue to advance to next day"
    - "Day splash shows new day number before Schedule Mode"
    - "Energy partially recovers overnight (+3, capped at max)"
    - "Intervention tokens reset with carry-over (unused + 3, max 4)"
    - "Previous schedule is preserved for next day"
  artifacts:
    - path: "src/components/summary/ContinueButton.tsx"
      provides: "Button to trigger day advancement"
      contains: "advanceToNextDay"
    - path: "src/components/summary/DaySplash.tsx"
      provides: "Day number splash screen"
      contains: "Day {day}"
  key_links:
    - from: "src/components/summary/ContinueButton.tsx"
      to: "src/stores/GameStore.ts"
      via: "calls advanceToNextDay"
      pattern: "advanceToNextDay"
    - from: "src/components/summary/DaySplash.tsx"
      to: "motion/react"
      via: "fade animation"
      pattern: "motion.*opacity"
---

<objective>
Complete the day loop by implementing the Continue button, day splash screen, and day transition logic. After clicking Continue, players see a brief "Day X" splash, then enter Schedule Mode with partially recovered energy and preserved schedule.

Purpose: Closes the core game loop. Without this, players cannot advance to the next day and the prototype cannot test the Schedule-Observe-Adjust cycle across multiple days.

Output: ContinueButton and DaySplash components, fully functional day transition with proper state reset.
</objective>

<execution_context>
@/Users/godstemning/.claude/get-shit-done/workflows/execute-plan.md
@/Users/godstemning/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-day-end-summary/04-CONTEXT.md
@.planning/phases/04-day-end-summary/04-RESEARCH.md
@src/stores/GameStore.ts
@src/stores/SimulationStore.ts
@src/components/summary/SummaryView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DaySplash screen component</name>
  <files>src/components/summary/DaySplash.tsx</files>
  <action>
Create src/components/summary/DaySplash.tsx:

1. Import motion from 'motion/react'
2. Define props interface:
   ```typescript
   interface DaySplashProps {
     day: number;
     onComplete: () => void;
   }
   ```
3. Build the component:
   - Fixed full-screen overlay (fixed inset-0 z-50)
   - Centered content (flex items-center justify-center)
   - Background: bg-base-100 (solid, not transparent)
   - Animate:
     - Container: initial opacity 1, animate to opacity 0 after delay
     - Text: initial scale 0.5 + opacity 0, animate to scale 1 + opacity 1
   - Display: "Day {day}" in large text (text-6xl font-bold)
   - Use onAnimationComplete on container to call onComplete callback
   - Timing per RESEARCH:
     - Text scale-up: duration 0.5s, ease 'easeOut'
     - Container fade-out: delay 1.5s, duration 0.5s (total 2s visible)

Example from RESEARCH:
```tsx
<motion.div
  className="fixed inset-0 z-50 flex items-center justify-center bg-base-100"
  initial={{ opacity: 1 }}
  animate={{ opacity: 0 }}
  transition={{ delay: 1.5, duration: 0.5 }}
  onAnimationComplete={onComplete}
>
  <motion.h1
    className="text-6xl font-bold"
    initial={{ scale: 0.5, opacity: 0 }}
    animate={{ scale: 1, opacity: 1 }}
    transition={{ duration: 0.5, ease: 'easeOut' }}
  >
    Day {day}
  </motion.h1>
</motion.div>
```
  </action>
  <verify>
- File exists at src/components/summary/DaySplash.tsx
- TypeScript compiles without errors
- Component renders fullscreen overlay with day number
  </verify>
  <done>
- DaySplash.tsx shows "Day X" with scale-up animation
- Fades out after 1.5s delay
- Calls onComplete when animation finishes
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ContinueButton and wire day transition flow</name>
  <files>src/components/summary/ContinueButton.tsx, src/components/summary/SummaryView.tsx, src/stores/SimulationStore.ts</files>
  <action>
1. Create src/components/summary/ContinueButton.tsx:
   - Import observer, useGameStore
   - Props: onContinue callback (called when button clicked)
   - Render a centered button with:
     - DaisyUI btn btn-primary btn-lg styling
     - Text: "Continue to Day {gameStore.currentDay + 1}"
     - onClick calls onContinue prop
   - Add brief instructional text above: "Ready for the next day?"

2. Update src/components/summary/SummaryView.tsx:
   - Import DaySplash and ContinueButton
   - Add state: showSplash (boolean, default false)
   - Add state: nextDay (number, to pass to splash)
   - Replace 'complete' step placeholder with ContinueButton
   - ContinueButton onContinue handler:
     - Set nextDay to gameStore.currentDay + 1
     - Set showSplash to true
     - Call gameStore.advanceToNextDay()
   - Conditionally render DaySplash when showSplash is true:
     - Pass nextDay as day prop
     - onComplete sets showSplash to false (the mode change already happened in advanceToNextDay, so splash just dismisses)
   - Reset step to 0 when SummaryView mounts (use useEffect with empty deps to reset currentStep)

3. Update src/stores/SimulationStore.ts:
   - Modify reset() method to accept optional tokenOverride parameter for token carry-over:
     - If tokenOverride is provided, use it; otherwise default to 3
     - This allows GameStore.advanceToNextDay() to calculate carry-over and pass it to reset()
   - Or alternatively: Add a separate method setInitialTokens(count: number) that GameStore can call after reset()

   Note: GameStore.advanceToNextDay() needs to handle token carry-over:
   - Get current unused tokens from SimulationStore (before reset)
   - Calculate new tokens: min(unusedTokens, 1) + 3 (max 4 per CONTEXT)
   - Call SimulationStore.reset() then set tokens

   If GameStore.advanceToNextDay() was not fully implemented in Plan 01, complete it now with proper SimulationStore integration.
  </action>
  <verify>
- ContinueButton exists and shows next day number
- Clicking Continue shows DaySplash, then transitions to Schedule Mode
- Intervention tokens properly carry over (1 unused = 4 total, 0 unused = 3 total)
- Schedule is preserved in Schedule Mode
- Energy has partial recovery (+3, capped at 10)
  </verify>
  <done>
- ContinueButton.tsx shows "Continue to Day X" button
- Clicking it triggers day splash then mode transition
- DaySplash shows and auto-dismisses after animation
- Full day loop: schedule -> observe -> summary -> continue -> day splash -> schedule (day+1)
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify complete day loop and polish</name>
  <files>src/components/summary/SummaryView.tsx</files>
  <action>
Final verification and polish pass:

1. Test the complete loop manually:
   - Start app (Day 1, Schedule Mode)
   - Assign activities to patients
   - Click "Start Day" -> enters Observe Mode
   - Watch day complete (or skip to end)
   - Verify transition to Summary Mode
   - Step through: Energy -> XP -> Discoveries -> Interventions -> Complete
   - Click Continue
   - Verify DaySplash shows "Day 2"
   - Verify Schedule Mode returns with:
     - Day counter shows 2
     - Previous activities still in schedule
     - Patient energy values show recovery (each got +3, capped at 10)
     - Intervention tokens reset appropriately

2. Edge case handling in SummaryView:
   - Ensure step resets when entering summary mode (not carrying over from previous day)
   - Use key prop on SummaryView in GameShell keyed by currentDay to force remount, OR
   - Use useEffect in SummaryView to reset currentStep when component mounts

3. Polish:
   - Add subtle animation when transitioning between steps (optional - use Motion if simple)
   - Ensure all text is readable and sections are well-spaced
   - Check mobile responsiveness (sections should stack vertically)
  </action>
  <verify>
- Complete day loop works: schedule -> observe -> summary -> continue -> schedule (day 2)
- Energy shows partial recovery (+3, max 10)
- Schedule preserved across days
- Tokens reset with carry-over
- Step navigation resets each day
- `npm run build` passes
  </verify>
  <done>
- Full day loop verified working
- State properly resets/persists per requirements
- Summary steps reset each day (no stale state)
- Phase 4 requirements END-01 through END-05 met
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. `npm run dev` runs successfully
3. Complete manual playthrough of 2+ days:
   - Day 1: schedule activities, observe, complete, summary, continue
   - Day 2: verify schedule preserved, energy recovered, tokens reset, day counter incremented
4. Verify all 5 END requirements:
   - END-01: Summary shows Overskudd change per patient (EnergySection)
   - END-02: Summary reveals discoveries (DiscoverySection)
   - END-03: Player can Continue to next day (ContinueButton)
   - END-04: Summary shows intervention tokens used (InterventionSection)
   - END-05: Summary shows skill/XP progress (XPSection)
</verification>

<success_criteria>
- DaySplash.tsx shows animated day number overlay
- ContinueButton.tsx triggers day advancement
- Day transition works: energy +3 (max 10), tokens reset with carry-over, schedule preserved
- Complete loop: schedule -> observe -> summary -> continue -> splash -> schedule
- All END-01 through END-05 requirements verifiable
- Multiple days can be played in sequence
</success_criteria>

<output>
After completion, create `.planning/phases/04-day-end-summary/04-03-SUMMARY.md`
</output>
