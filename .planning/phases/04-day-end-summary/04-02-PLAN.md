---
phase: 04-day-end-summary
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/summary/EnergySection.tsx
  - src/components/summary/XPSection.tsx
  - src/components/summary/DiscoverySection.tsx
  - src/components/summary/InterventionSection.tsx
  - src/components/summary/SummaryView.tsx
autonomous: true

must_haves:
  truths:
    - "Energy section shows start/end/change bars for each patient"
    - "XP section shows per-activity gains for each patient"
    - "Discovery cards flip to reveal content"
    - "Intervention section shows tokens used count"
  artifacts:
    - path: "src/components/summary/EnergySection.tsx"
      provides: "Energy bar chart display per patient"
      contains: "startEnergy"
    - path: "src/components/summary/XPSection.tsx"
      provides: "XP gains list per patient"
      contains: "XPGain"
    - path: "src/components/summary/DiscoverySection.tsx"
      provides: "Card flip animation for discoveries"
      contains: "rotateY"
    - path: "src/components/summary/InterventionSection.tsx"
      provides: "Token usage summary"
      contains: "interventionTokens"
  key_links:
    - from: "src/components/summary/EnergySection.tsx"
      to: "src/stores/GameStore.ts"
      via: "reads startEnergySnapshot"
      pattern: "startEnergySnapshot"
    - from: "src/components/summary/DiscoverySection.tsx"
      to: "motion/react"
      via: "motion component for flip"
      pattern: "motion.*rotateY"
---

<objective>
Build the four content sections for the Day End Summary: EnergySection (mini bar charts), XPSection (activity gains), DiscoverySection (card flip reveals), and InterventionSection (token count).

Purpose: These sections deliver the actual feedback to players about what happened during the day. Without them, the summary is an empty shell.

Output: Four section components wired into SummaryView, rendering real energy data and mock XP/discovery data (Phase 5 will make XP/discoveries dynamic).
</objective>

<execution_context>
@/Users/godstemning/.claude/get-shit-done/workflows/execute-plan.md
@/Users/godstemning/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-day-end-summary/04-CONTEXT.md
@.planning/phases/04-day-end-summary/04-RESEARCH.md
@src/stores/GameStore.ts
@src/stores/SimulationStore.ts
@src/components/summary/SummaryView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EnergySection with mini bar charts</name>
  <files>src/components/summary/EnergySection.tsx</files>
  <action>
Create src/components/summary/EnergySection.tsx:

1. Import observer from mobx-react-lite, useGameStore
2. Build EnergyBar component (internal, not exported):
   - Props: patientName, startEnergy, endEnergy, maxEnergy (default 10)
   - Calculate startPercent and endPercent as percentages of maxEnergy
   - Calculate change = endEnergy - startEnergy
   - Render two horizontal bars (Start and End) using Tailwind:
     - Container: h-3 rounded-full bg-base-300
     - Fill: h-full rounded-full, width as percentage
     - Start bar: bg-info
     - End bar: bg-success if >= start, bg-error if < start
   - Show numeric values: startEnergy on left, endEnergy on right
   - Show change indicator: +N in text-success or -N in text-error

3. Build EnergySection component (exported):
   - Get gameStore via useGameStore()
   - For each patient, get:
     - startEnergy from gameStore.startEnergySnapshot.get(patient.id) ?? patient.energy
     - endEnergy from patient.energy (current value)
   - Render heading "Overskudd Changes" (use Norwegian term per game theme)
   - Render grid of EnergyBar components (one per patient)
   - Use grid-cols-1 md:grid-cols-3 for responsive layout

CSS reference from RESEARCH:
```tsx
<div className="h-3 flex-1 rounded-full bg-base-300">
  <div
    className="h-full rounded-full bg-info transition-all"
    style={{ width: `${percent}%` }}
  />
</div>
```
  </action>
  <verify>
- File exists and exports EnergySection
- Component shows bar charts when startEnergySnapshot has data
- TypeScript compiles without errors
  </verify>
  <done>
- EnergySection.tsx displays mini bar charts for each patient
- Shows start (info color), end (success/error based on change), and change value
- Reads from gameStore.startEnergySnapshot for start values
  </done>
</task>

<task type="auto">
  <name>Task 2: Create XPSection and InterventionSection</name>
  <files>src/components/summary/XPSection.tsx, src/components/summary/InterventionSection.tsx</files>
  <action>
1. Create src/components/summary/XPSection.tsx:
   - Import observer, useGameStore
   - Create getMockXPGains function that returns XPGain[] based on patient's scheduled activities:
     - For each time slot, if patient has activity assigned, add { activityId, activityName, xp: 15 }
     - This reads from gameStore.schedule and gameStore.activities
   - Render heading "Skills Practiced"
   - Render grid of patient cards (grid-cols-1 md:grid-cols-3)
   - Each card shows:
     - Patient name
     - List of activities with "+15 XP" for each (mock value - Phase 5 implements real formula)
   - Use DaisyUI card styling (card bg-base-200 p-4)

2. Create src/components/summary/InterventionSection.tsx:
   - Import observer, useSimulationStore
   - Calculate tokensUsed = 3 - simulationStore.interventionTokens (initial is 3)
   - Render heading "Interventions"
   - Use DaisyUI stat component:
     ```tsx
     <div className="stats shadow">
       <div className="stat">
         <div className="stat-title">Tokens Used</div>
         <div className="stat-value">{tokensUsed}/3</div>
         <div className="stat-desc">
           {tokensUsed === 0 ? 'No interventions needed!' : `Redirected ${tokensUsed} ${tokensUsed === 1 ? 'activity' : 'activities'}`}
         </div>
       </div>
     </div>
     ```
   - Center the stats component
  </action>
  <verify>
- Both files exist and export their components
- XPSection shows activities completed with mock XP values
- InterventionSection shows token usage count
- TypeScript compiles
  </verify>
  <done>
- XPSection.tsx shows per-activity XP gains (mock 15 XP per activity)
- InterventionSection.tsx shows tokens used out of 3
- Both components read from appropriate stores
  </done>
</task>

<task type="auto">
  <name>Task 3: Create DiscoverySection with card flip and wire all sections into SummaryView</name>
  <files>src/components/summary/DiscoverySection.tsx, src/components/summary/SummaryView.tsx</files>
  <action>
1. Create src/components/summary/DiscoverySection.tsx:
   - Import useState, useEffect from react, motion from 'motion/react', observer from mobx-react-lite
   - Define mock discoveries array (Phase 5 will make dynamic):
     ```typescript
     const mockDiscoveries: Discovery[] = [
       { id: 'd1', patientId: 'patient-1', text: 'Elling finds therapy sessions draining' },
       { id: 'd2', patientId: 'patient-3', text: 'Nora seems to enjoy social activities' },
     ];
     ```
   - Create DiscoveryCard component:
     - Props: discovery, isRevealed
     - Container div with perspective: '1000px'
     - motion.div child with rotateY animation (0 when hidden, 180 when revealed)
     - Two absolute positioned faces:
       - Front: "?" mystery card (bg-base-200, backfaceVisibility: hidden)
       - Back: Discovery text (bg-accent, backfaceVisibility: hidden, transform: rotateY(180deg))
     - Animation: duration 0.6s, ease [0.4, 0, 0.2, 1]
   - DiscoverySection component:
     - Track revealedIds as Set<string> and currentIndex
     - useEffect for auto-reveal: setTimeout every 800ms to reveal next card
     - Render heading "Discoveries"
     - Render flex wrap of DiscoveryCards
     - Handle empty state: "No new discoveries today" if mockDiscoveries is empty

2. Update src/components/summary/SummaryView.tsx:
   - Import EnergySection, XPSection, DiscoverySection, InterventionSection
   - Replace placeholder content with actual components:
     - 'energy' step: <EnergySection />
     - 'xp' step: <XPSection />
     - 'discoveries' step: <DiscoverySection />
     - 'interventions' step: <InterventionSection />
     - 'complete' step: Placeholder text "Ready to continue" (ContinueButton added in Plan 03)
  </action>
  <verify>
- DiscoverySection exists with card flip animation
- Cards auto-reveal with staggered timing
- SummaryView renders all four sections at appropriate steps
- `npm run dev` shows sections cycling through properly
  </verify>
  <done>
- DiscoverySection.tsx implements card flip with Motion rotateY
- Auto-reveal with 800ms stagger between cards
- All four sections (Energy, XP, Discoveries, Interventions) render in SummaryView
- Step navigation shows correct content for each step
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. `npm run dev` runs successfully
3. Manual test: Complete a day, enter summary, step through all sections
4. Verify: Energy bars show start/end values, XP shows activities, discoveries flip, interventions show count
</verification>

<success_criteria>
- Four section components exist in src/components/summary/
- EnergySection shows mini bar charts with real energy data from snapshot
- XPSection shows mock XP gains per activity
- DiscoverySection has working card flip animation with Motion
- InterventionSection shows token usage
- All sections render correctly when stepping through SummaryView
</success_criteria>

<output>
After completion, create `.planning/phases/04-day-end-summary/04-02-SUMMARY.md`
</output>
