---
phase: 04-day-end-summary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/types.ts
  - src/stores/GameStore.ts
  - src/components/summary/SummaryView.tsx
autonomous: true

must_haves:
  truths:
    - "GameStore can capture energy snapshot at day start"
    - "GameStore can advance to next day with partial energy recovery"
    - "SummaryView renders step-through navigation"
  artifacts:
    - path: "src/models/types.ts"
      provides: "Discovery and XPGain type definitions"
      contains: "interface Discovery"
    - path: "src/stores/GameStore.ts"
      provides: "startEnergySnapshot Map and advanceToNextDay action"
      contains: "advanceToNextDay"
    - path: "src/components/summary/SummaryView.tsx"
      provides: "Step-through container component"
      contains: "currentStep"
  key_links:
    - from: "src/stores/GameStore.ts"
      to: "src/models/types.ts"
      via: "imports Discovery, XPGain types"
      pattern: "import.*Discovery.*from"
---

<objective>
Create the foundation for the Day End Summary phase: MobX store enhancements for tracking energy snapshots and day advancement, plus the SummaryView container component with step-through navigation logic.

Purpose: Establishes the state management and UI container needed for all summary sections. Without this, we cannot track energy changes (start vs end) or advance to the next day.

Output: Updated GameStore with snapshot/advance capabilities, new types for discoveries and XP, and a SummaryView shell component with step navigation.
</objective>

<execution_context>
@/Users/godstemning/.claude/get-shit-done/workflows/execute-plan.md
@/Users/godstemning/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-day-end-summary/04-CONTEXT.md
@.planning/phases/04-day-end-summary/04-RESEARCH.md
@src/stores/GameStore.ts
@src/stores/SimulationStore.ts
@src/models/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add summary types and enhance GameStore</name>
  <files>src/models/types.ts, src/stores/GameStore.ts</files>
  <action>
1. In src/models/types.ts, add these interfaces:
```typescript
export interface Discovery {
  id: string;
  patientId: string;
  text: string; // e.g., "Ada seems to enjoy cooking"
}

export interface XPGain {
  activityId: string;
  activityName: string;
  xp: number;
}
```

2. In src/stores/GameStore.ts, add:
- Import observable from mobx (already imported)
- Add `startEnergySnapshot: Map<string, number> = observable.map()` field
- Add `captureStartEnergy()` method that copies each patient's current energy into startEnergySnapshot
- Add `advanceToNextDay()` method that:
  - Increments currentDay
  - Applies partial energy recovery: each patient gets +3 energy, capped at maxEnergy (10)
  - Resets interventionTokens: get 3 fresh, but can keep 1 unused from previous day (max 4). Access SimulationStore via getSimulationStore().
  - Clears startEnergySnapshot
  - Keeps schedule intact (do NOT clear - per CONTEXT: "previous day's schedule pre-filled")
  - Sets currentMode to 'schedule'
  </action>
  <verify>
- TypeScript compiles: `npm run build` passes
- GameStore has captureStartEnergy and advanceToNextDay methods
- types.ts exports Discovery and XPGain interfaces
  </verify>
  <done>
- Discovery and XPGain types exported from types.ts
- GameStore.captureStartEnergy() snapshots all patient energy values
- GameStore.advanceToNextDay() increments day, applies +3 recovery (max 10), handles token carry-over, preserves schedule, switches to schedule mode
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SummaryView container with step-through logic</name>
  <files>src/components/summary/SummaryView.tsx</files>
  <action>
Create src/components/summary/SummaryView.tsx:

1. Create the summary folder: src/components/summary/

2. Build SummaryView.tsx with:
- Import useState from react, observer from mobx-react-lite
- Define SummaryStep type: 'energy' | 'xp' | 'discoveries' | 'interventions' | 'complete'
- Define STEPS array with these 5 steps
- Track currentStep as index into STEPS array
- Implement nextStep() that increments currentStep if not at end
- Implement skipToEnd() that jumps to 'complete' step
- Render:
  - DaisyUI steps indicator showing progress through STEPS
  - Conditional rendering placeholder for each step (just show step name for now - sections added in Plan 02)
  - Bottom navigation with "Skip" button (btn-ghost, calls skipToEnd) and "Continue" button (btn-primary, calls nextStep, hidden on 'complete' step)
- Use observer wrapper for MobX reactivity
- Export as named export

Layout should be a centered card-style container with flex-col, gap-6, padding. Use DaisyUI card component for main container.

Note: The actual section components (EnergySection, XPSection, etc.) will be added in Plan 02. For now, render placeholder text like "Energy changes will appear here" for each step.
  </action>
  <verify>
- File exists at src/components/summary/SummaryView.tsx
- Component renders without errors when imported
- Step indicator shows 5 steps
- Continue button advances through steps
- Skip button jumps to complete
  </verify>
  <done>
- SummaryView.tsx exists with step-through navigation
- 5 steps defined: energy, xp, discoveries, interventions, complete
- Steps indicator shows current progress
- Continue advances, Skip jumps to end
- Placeholder content renders for each step
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire SummaryView into game mode switching</name>
  <files>src/components/GameShell.tsx, src/components/observe/ObserveView.tsx</files>
  <action>
1. In src/components/GameShell.tsx:
- Import SummaryView from './summary/SummaryView'
- Add rendering for summary mode alongside schedule and observe:
  ```tsx
  {gameStore.currentMode === 'summary' && <SummaryView />}
  ```
- Ensure sidebar is hidden in summary mode (already handled by showSidebar check)

2. In src/components/observe/ObserveView.tsx:
- Find where the day completion is detected (isDayComplete)
- When day completes and user is ready to proceed (e.g., after "End Day" button or auto-transition):
  - Call gameStore.setMode('summary') to transition to summary
- Note: The current ObserveView may already auto-pause on day complete. Add a way to proceed to summary - either auto-transition after a short delay, or add an "End Day" / "View Summary" button that appears when isDayComplete is true.
- Before transitioning to observe mode (in the StartDayModal or wherever that transition happens), call gameStore.captureStartEnergy() to snapshot energy values.

Look at StartDayModal.tsx to find where observe mode transition happens, and add captureStartEnergy() call there.
  </action>
  <verify>
- `npm run dev` starts without errors
- Click through schedule -> observe -> complete day -> summary flow works
- SummaryView renders when mode is 'summary'
- Energy snapshot is captured before observe mode starts
  </verify>
  <done>
- GameShell renders SummaryView when currentMode === 'summary'
- ObserveView transitions to summary mode when day completes
- captureStartEnergy() called before entering observe mode
- Full mode flow: schedule -> observe -> summary works
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes without type errors
2. `npm run dev` starts successfully
3. Manual test: Start app -> schedule activities -> click Start Day -> watch day complete -> see SummaryView appear
4. Check that step navigation works (Continue advances, Skip jumps to end)
</verification>

<success_criteria>
- Types Discovery and XPGain exist in types.ts
- GameStore has startEnergySnapshot Map with captureStartEnergy() and advanceToNextDay() methods
- SummaryView exists with 5-step navigation (energy, xp, discoveries, interventions, complete)
- Mode transition: schedule -> observe (captures energy) -> summary (on day complete) works
- Build passes, dev server runs
</success_criteria>

<output>
After completion, create `.planning/phases/04-day-end-summary/04-01-SUMMARY.md`
</output>
