---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/models/types.ts
  - src/models/Patient.ts
  - src/models/Activity.ts
  - src/stores/GameStore.ts
  - src/stores/UIStore.ts
  - src/stores/RootStore.ts
autonomous: true

must_haves:
  truths:
    - "GameStore contains 3 patients with distinct MTG colors and energy values"
    - "GameStore contains 8 activities with energy cost/gain values"
    - "GameStore tracks current day (starting at 1) and game mode"
    - "Store hooks provide access to stores from React components"
  artifacts:
    - path: "src/models/types.ts"
      provides: "GameMode, TimeSlot, MTGColor type definitions"
      exports: ["GameMode", "TimeSlot", "MTGColor", "PatientData", "ActivityData"]
    - path: "src/models/Patient.ts"
      provides: "Patient class with observable energy"
      exports: ["Patient"]
      contains: "makeAutoObservable"
    - path: "src/models/Activity.ts"
      provides: "Activity class with energy cost"
      exports: ["Activity"]
    - path: "src/stores/GameStore.ts"
      provides: "Domain state store"
      exports: ["GameStore", "useGameStore"]
      contains: "patients"
    - path: "src/stores/UIStore.ts"
      provides: "UI state store"
      exports: ["UIStore", "useUIStore"]
    - path: "src/stores/RootStore.ts"
      provides: "Store composition"
      exports: ["RootStore", "useRootStore"]
  key_links:
    - from: "src/stores/GameStore.ts"
      to: "src/models/Patient.ts"
      via: "import and instantiation"
      pattern: "new Patient"
    - from: "src/stores/GameStore.ts"
      to: "src/models/Activity.ts"
      via: "import and instantiation"
      pattern: "new Activity"
---

<objective>
Create MobX stores and TypeScript data models for patients and activities with initial hardcoded data.

Purpose: Establish the domain model and state management layer that UI components will consume.
Output: GameStore with 3 patients, 8 activities, day/mode tracking. UIStore for presentation state. RootStore composing both.
</objective>

<execution_context>
@/Users/godstemning/.claude/get-shit-done/workflows/execute-plan.md
@/Users/godstemning/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeScript type definitions</name>
  <files>src/models/types.ts</files>
  <action>
Create src/models/types.ts with all shared types:

```typescript
export type GameMode = 'schedule' | 'observe' | 'summary';
export type TimeSlot = 'morning' | 'afternoon' | 'evening';
export type MTGColor = 'white' | 'blue' | 'black' | 'red' | 'green';

export interface PatientData {
  id: string;
  name: string;
  energy: number;
  primaryColor: MTGColor;
  secondaryColor?: MTGColor;
}

export interface ActivityData {
  id: string;
  name: string;
  energyCost: number; // negative = costs energy, positive = restores
  color: MTGColor;    // activity's natural affinity
}
```

These types are used by both models and stores. PatientData/ActivityData are constructor inputs; Patient/Activity classes are the runtime objects.
  </action>
  <verify>
TypeScript compiles without errors: npx tsc --noEmit
File exports all required types
  </verify>
  <done>
types.ts exports GameMode, TimeSlot, MTGColor, PatientData, ActivityData with correct definitions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Patient and Activity observable classes</name>
  <files>src/models/Patient.ts, src/models/Activity.ts</files>
  <action>
Create src/models/Patient.ts:
- Import makeAutoObservable from 'mobx'
- Import MTGColor, PatientData from './types'
- Patient class with:
  - id: string, name: string, energy: number, maxEnergy: 10
  - primaryColor: MTGColor, secondaryColor?: MTGColor
  - Constructor takes PatientData, calls makeAutoObservable(this)
  - Computed getter energyPercent: (energy / maxEnergy) * 100
  - Computed getter energyStatus: 'high' (>=6), 'medium' (>=3), 'low' (<3)

Create src/models/Activity.ts:
- Import makeAutoObservable from 'mobx'
- Import MTGColor, ActivityData from './types'
- Activity class with:
  - id: string, name: string, energyCost: number, color: MTGColor
  - Constructor takes ActivityData, calls makeAutoObservable(this)
  - Computed getter isRestoring: energyCost > 0
  - Computed getter formattedCost: returns "+N" or "-N" string for display
  </action>
  <verify>
npx tsc --noEmit passes
Patient and Activity classes can be instantiated
  </verify>
  <done>
Patient class with energy tracking and MTG colors. Activity class with energy cost and display helpers. Both use makeAutoObservable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create MobX stores with initial data</name>
  <files>src/stores/GameStore.ts, src/stores/UIStore.ts, src/stores/RootStore.ts</files>
  <action>
Create src/stores/GameStore.ts:
- Import makeAutoObservable from 'mobx'
- Import Patient, Activity, GameMode, TimeSlot
- GameStore class with:
  - currentDay: number = 1
  - currentTimeSlot: TimeSlot = 'morning'
  - currentMode: GameMode = 'schedule'
  - patients: Patient[] = []
  - activities: Activity[] = []
  - Constructor calls makeAutoObservable(this), then initializeGame()
  - initializeGame() creates:
    - 3 patients: Elling (blue/green, energy 7), Kjell-Bjarne (green/white, energy 5), Nora (red/blue, energy 8)
    - 8 activities: Cooking (-2, green), Therapy (-3, blue), Gardening (-1, green), Rest (+2, white), Reading (-1, blue), Social Hour (-2, red), Crafts (-1, red), Exercise (-2, green)
  - setMode(mode: GameMode) action
- Export singleton: const gameStore = new GameStore()
- Export hook: useGameStore = () => gameStore
- Export getter: getGameStore = () => gameStore (for non-React access)

Create src/stores/UIStore.ts:
- Import makeAutoObservable from 'mobx'
- UIStore class with:
  - selectedPatientId: string | null = null
  - sidebarExpanded: boolean = true
  - Constructor calls makeAutoObservable(this)
  - Actions: setSelectedPatient(id), toggleSidebar()
- Export singleton, hook (useUIStore), getter (getUIStore)

Create src/stores/RootStore.ts:
- Import GameStore, UIStore
- RootStore class composing gameStore and uiStore references
- Export singleton rootStore
- Export useRootStore hook
  </action>
  <verify>
npx tsc --noEmit passes
Import stores in a test: stores are singletons, patients.length === 3, activities.length === 8
  </verify>
  <done>
GameStore with 3 patients (distinct colors/energy), 8 activities, day 1, schedule mode. UIStore for selection state. RootStore composing both. All stores accessible via hooks.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no type errors
2. Import GameStore in App.tsx temporarily to verify:
   - gameStore.patients.length === 3
   - gameStore.activities.length === 8
   - gameStore.currentDay === 1
   - gameStore.currentMode === 'schedule'
3. Patient energyStatus computed property returns correct values
4. Activity formattedCost returns "+2" for Rest, "-2" for Cooking
</verification>

<success_criteria>
- All type definitions exported and usable
- Patient class tracks energy with computed status
- Activity class has energy cost with formatted display
- GameStore initialized with 3 patients and 8 activities
- UIStore ready for selection state management
- Store hooks ready for React component consumption
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
