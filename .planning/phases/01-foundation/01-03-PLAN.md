---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/App.tsx
  - src/components/GameShell.tsx
  - src/components/PatientCard.tsx
  - src/components/PatientGrid.tsx
  - src/components/ActivityList.tsx
  - src/components/DayHeader.tsx
autonomous: true

must_haves:
  truths:
    - "App displays current game mode (Schedule/Observe/Summary) in header"
    - "Player sees 3 patient cards with names, energy bars, and MTG color indicators"
    - "Activity list shows 8 activities with energy cost/gain values"
    - "Day number (Day 1) and time slot (Morning) visible in header"
    - "Activity sidebar only visible in Schedule mode"
  artifacts:
    - path: "src/components/GameShell.tsx"
      provides: "Mode-based layout container"
      exports: ["GameShell"]
      contains: "currentMode"
    - path: "src/components/PatientCard.tsx"
      provides: "Individual patient display"
      exports: ["PatientCard"]
      contains: "energyStatus"
    - path: "src/components/PatientGrid.tsx"
      provides: "Grid of patient cards"
      exports: ["PatientGrid"]
    - path: "src/components/ActivityList.tsx"
      provides: "Sidebar with activities"
      exports: ["ActivityList"]
      contains: "formattedCost"
    - path: "src/components/DayHeader.tsx"
      provides: "Mode, day, time display"
      exports: ["DayHeader"]
  key_links:
    - from: "src/components/GameShell.tsx"
      to: "src/stores/GameStore.ts"
      via: "useGameStore hook"
      pattern: "useGameStore"
    - from: "src/components/PatientCard.tsx"
      to: "src/models/Patient.ts"
      via: "patient prop type"
      pattern: "Patient"
    - from: "src/components/ActivityList.tsx"
      to: "src/stores/GameStore.ts"
      via: "useGameStore for activities"
      pattern: "gameStore\\.activities"
---

<objective>
Create the UI shell with mode switching, patient cards grid, activity sidebar, and day header.

Purpose: Deliver the visual foundation that satisfies all Phase 1 success criteria - the player can see game mode, patients with energy, activities with costs, and day/time.
Output: Complete Phase 1 UI with GameShell layout, PatientGrid, ActivityList sidebar, DayHeader.
</objective>

<execution_context>
@/Users/godstemning/.claude/get-shit-done/workflows/execute-plan.md
@/Users/godstemning/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GameShell layout and DayHeader</name>
  <files>src/components/GameShell.tsx, src/components/DayHeader.tsx</files>
  <action>
Create src/components/DayHeader.tsx:
- Import useGameStore from stores
- Display in a horizontal bar:
  - Current mode as badge/text (Schedule Mode / Observe Mode / Summary)
  - Day number (Day 1)
  - Current time slot (Morning / Afternoon / Evening)
- Use DaisyUI badge component for mode indicator
- Use Tailwind flex layout, dark theme appropriate colors
- Mode badge should have distinct colors per mode (schedule=primary, observe=secondary, summary=accent)

Create src/components/GameShell.tsx:
- Import useGameStore from stores
- Import DayHeader, PatientGrid, ActivityList (create placeholders if not yet built)
- Layout structure:
  - Full viewport height (h-screen)
  - Flex column: header at top, content below
  - Content area: flex row with main area (patient grid) and sidebar (activity list)
  - Sidebar on RIGHT side (per CONTEXT.md)
  - Sidebar only visible when currentMode === 'schedule'
- Use DaisyUI/Tailwind dark theme colors (bg-base-100, bg-base-200)
- Game-like aesthetic: bold borders, clear sections

Replace App.tsx content to render GameShell instead of placeholder.
  </action>
  <verify>
npm run dev shows GameShell with header and layout structure
Mode indicator shows "Schedule Mode"
Day 1 and Morning visible in header
  </verify>
  <done>
GameShell provides layout with header + main + sidebar. DayHeader displays mode badge, day number, time slot. Sidebar conditionally rendered based on mode.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PatientCard and PatientGrid</name>
  <files>src/components/PatientCard.tsx, src/components/PatientGrid.tsx</files>
  <action>
Create src/components/PatientCard.tsx:
- Props: patient: Patient (from models)
- Import clsx for conditional classes
- Layout using DaisyUI card component:
  - Colored left border showing primary MTG color (use border-l-4)
  - Patient name as card title
  - Energy display: number + progress bar
  - Progress bar color based on energyStatus:
    - 'high' (>=6): progress-success (green)
    - 'medium' (>=3): progress-warning (yellow)
    - 'low' (<3): progress-error (red)
- MTG color to Tailwind border mapping:
  - white: border-amber-100
  - blue: border-blue-500
  - black: border-gray-800
  - red: border-red-500
  - green: border-green-500
- Card should have visual punch - shadow, clear borders, readable text on dark bg

Create src/components/PatientGrid.tsx:
- Import useGameStore
- Import PatientCard
- Render grid of PatientCards for all patients in store
- Use CSS grid: 2 columns on larger screens, 1 column on mobile
- Grid gap for spacing between cards
- This component maps over gameStore.patients and renders PatientCard for each
  </action>
  <verify>
Browser shows 3 patient cards in grid
Each card shows: name, energy number, colored progress bar
Border colors match primary MTG color (Elling=blue, Kjell-Bjarne=green, Nora=red)
Energy bar colors match status (Nora high=green, Kjell-Bjarne medium=yellow)
  </verify>
  <done>
PatientCard displays name, energy bar with status colors, MTG color border. PatientGrid renders 3 patients in responsive grid layout.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create ActivityList sidebar</name>
  <files>src/components/ActivityList.tsx</files>
  <action>
Create src/components/ActivityList.tsx:
- Import useGameStore
- Import Activity type and lucide-react icons (ArrowUp, ArrowDown or similar)
- Render vertical list of all activities from store
- Each activity row shows:
  - Activity name
  - Energy cost/gain with visual indicator:
    - Positive (gain): green up arrow + "+N" text
    - Negative (cost): red down arrow + "-N" text (or just "N")
  - Use Activity.formattedCost computed property for the text
- Subtle background color per row for readability
- Consider MTG color hint for activity (small colored dot or subtle border)
- Scrollable if list grows (overflow-y-auto)
- Sidebar width should be appropriate for content (~250-300px)
- Dark theme styling consistent with rest of app
  </action>
  <verify>
Browser shows activity sidebar on right
8 activities visible with names
Energy values shown with directional indicators (Rest shows +2 green, Cooking shows -2 red)
Sidebar scrolls if content overflows
Sidebar hidden when mode !== 'schedule' (test by temporarily changing store)
  </verify>
  <done>
ActivityList displays 8 activities with energy cost/gain indicators. Visual distinction between energy-restoring and energy-costing activities. Sidebar integrates into GameShell layout.
  </done>
</task>

</tasks>

<verification>
**Phase 1 Success Criteria Check:**

1. App loads and displays current game mode (Schedule/Observe/Summary)
   - DayHeader shows mode badge with "Schedule Mode"

2. Player can see 2-3 patient cards with names, energy values, and color indicators
   - PatientGrid shows 3 cards (Elling, Kjell-Bjarne, Nora)
   - Each card has name, energy number, energy bar, MTG color border

3. Activity list shows 8-10 activities with energy cost/gain values
   - ActivityList sidebar shows 8 activities
   - Each shows name + energy with visual indicator

4. Day number and time slot are visible in UI
   - DayHeader shows "Day 1" and "Morning"

**Visual Verification:**
- Dark theme applied (dracula)
- Game-like aesthetic: bold colors, clear visual hierarchy
- Responsive layout works on different screen sizes
</verification>

<success_criteria>
- All Phase 1 requirements (STATE-01 through STATE-04) have visible UI representation
- All 4 success criteria from ROADMAP.md are met
- Layout matches CONTEXT.md decisions (sidebar on right, header with mode/day/time)
- Dark game-like aesthetic with bold colors
- Components properly consume MobX stores via hooks
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
